# @joschavai/validate-utils

Simple utility di validazione per input comuni.

## Installazione

```bash
yarn add @joschavai/validate-utils
```

## Funzioni

- `isValidEmail(value)`
- `isStrongPassword(value)`
- `hasMinLength(value, minLength)`
- `isNumericString(value)`